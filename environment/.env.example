# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=claude_demo

# JWT
JWT_ACCESS_SECRET=your-access-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# App
NODE_ENV=development
PORT=3000
API_VERSION=v1
CORS_ORIGIN=*

# Available environments: dev, stg, uat, prod
# Copy this file as .env.{environment} and adjust values accordingly
# Example: cp .env.example .env.dev
#
# Use the corresponding script to start the server:
# - npm run start:dev  (development - port 3000)
# - npm run start:stg  (staging - port 3001)
# - npm run start:uat  (user acceptance testing - port 3002)
# - npm run start:prod (production - port 3000)
